import locationService from '@/api/location'

export default {
    data() {
        return {
            currentPage: 1,
            pageSize: 10,
            totalSize: 0,
        }
    },
    methods: {

    }
}